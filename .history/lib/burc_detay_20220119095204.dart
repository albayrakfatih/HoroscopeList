import 'package:burc_rehberi/model/burc.dart';

class BurcDetay extends StatelessWidget {
  final Burc secilenBurc;
  const BurcDetay({required Burc Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
